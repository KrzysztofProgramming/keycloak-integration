spring:
  application:
    name: eureka-server

server:
  port: ${EUREKA_SERVER_PORT:8082}

#eureka:
#  client:
#    register-with-eureka: false
#    fetch-registry: false
#    serviceUrl:
#      defaultZone: http://localhost:8082/eureka

logging.level.com.netflix:
  eureka: OFF
  discovery: OFF
